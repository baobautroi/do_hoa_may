╔═══════════════════════════════════════════════════════════════════╗
║          QUICK TEST - Demo với Simple Tree Model                  ║
╚═══════════════════════════════════════════════════════════════════╝

Tôi đã tạo một simple tree model để bạn test ngay!

📁 File: models/demo_tree.obj (đã tạo sẵn)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 BƯỚC 1: Sửa code để load demo model

Mở file: pickleball_scene.cpp

Tìm dòng (~1922):
    bool treeLoaded = treeModel.loadModel("models/tree.obj");

Sửa thành:
    bool treeLoaded = treeModel.loadModel("models/demo_tree.obj");

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔨 BƯỚC 2: Compile lại

    compile_with_assimp.bat

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶️  BƯỚC 3: Chạy

    RUN_GAME.bat

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ BẠN SẼ THẤY:

Console output:
    === Loading 3D Models ===
    Model loaded successfully: models/demo_tree.obj
    Meshes: 1
    ✓ 3D Models loaded successfully!
      - Tree model loaded

Và trong scene:
    → Cây sẽ là PYRAMID đơn giản thay vì SPHERES!
    → Vẫn có wind sway animation
    → Đây là model 3D thật!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎨 SAU ĐÓ: Tải models đẹp hơn!

Bây giờ bạn đã biết nó hoạt động, hãy:

1. Tìm tree model đẹp hơn trên Sketchfab
2. Download dạng .obj
3. Đổi tên thành tree.obj (hoặc sửa lại code)
4. Đặt vào models/
5. Compile và chạy lại!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 TIP: Nếu model quá lớn/nhỏ

Trong pickleball_scene.cpp, dòng ~307:

    treeModel.render(0, 0, 0, 1.5f);
                              ^^^^
                              Scale factor

Thử:
    - 0.1f = Rất nhỏ
    - 0.5f = Nhỏ
    - 1.0f = Normal
    - 2.0f = Lớn
    - 5.0f = Rất lớn

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 HOẶC: Rename demo_tree.obj → tree.obj

Nếu không muốn sửa code:

    cd models
    copy demo_tree.obj tree.obj

Rồi compile và run như bình thường!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 KẾT QUẢ MONG ĐỢI:

Console:
    ✓ 3D Models loaded successfully!
      - Tree model loaded

Scene:  
    🌲 Cây là pyramid thay vì sphere
    💨 Vẫn có wind animation
    ✨ Đây là model 3D thật đấy!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 READY TO TEST!

Chạy ngay:  RUN_GAME.bat

Hoặc sửa code load demo_tree.obj như hướng dẫn ở trên!

╚═══════════════════════════════════════════════════════════════════╝
